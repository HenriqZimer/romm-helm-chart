{{- if .Values.romm.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "romm.fullname" . }}
  labels:
    {{- include "romm.labels" . | nindent 4 }}
    app.kubernetes.io/component: romm
  {{- with .Values.romm.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if .Values.romm.ingress.className }}
  ingressClassName: {{ .Values.romm.ingress.className }}
  {{- end }}
  {{- if .Values.romm.ingress.tls }}
  tls:
    {{- range .Values.romm.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.romm.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            pathType: {{ .pathType }}
            backend:
              service:
                name: {{ include "romm.fullname" $ }}
                port:
                  number: {{ $.Values.romm.service.port }}
          {{- end }}
    {{- end }}
{{- end }}
